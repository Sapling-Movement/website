---
layout: "base.njk"
pagination:
  data: allPages
  size: 1
  resolve: values
  alias: _data
permalink: "{{ _data.pageBase.fullSlug }}/index.html"
eleventyComputed:
  title: "{{ _data.pageBase.title }}"
  lang: "{{ _data.__lang }}"
---

{{ _data.pageBase.title }}
